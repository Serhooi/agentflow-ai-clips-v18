# AgentFlow AI Clips - Environment Variables Example
# Скопируйте этот файл в .env и заполните своими значениями

# OpenAI API Key (обязательно)
OPENAI_API_KEY=your-openai-api-key-here

# Redis URL (опционально, для масштабирования)
REDIS_URL=redis://localhost:6379/0

# Supabase (опционально, для внешнего хранения)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Порт приложения (по умолчанию 8000)
PORT=8000

# Максимальный размер файла в МБ (по умолчанию 250)
MAX_FILE_SIZE_MB=250

# Длительность клипов в секундах (гибкие границы)
CLIP_MIN_DURATION=40
CLIP_MAX_DURATION=80

# Таймаут для ffmpeg (множитель от длительности клипа)
FFMPEG_TIMEOUT_MULTIPLIER=4

# Настройки субтитров
SUBTITLES_UPPERCASE=true
SUBTITLES_WORDS_PER_GROUP=7

# Worker ID (для background workers)
WORKER_ID=worker-1

# Render.com переменные (автоматически устанавливаются)
# RENDER=true
# RENDER_SERVICE_ID=srv-xxxxx
# RENDER_SERVICE_NAME=your-service-name